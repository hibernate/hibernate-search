#This is to let Maven properties be injected in the file, making it easier to run tests in the IDE as well
dependency.version.HibernateSearch = ${project.version}
dependency.version.Lucene = ${luceneVersion}
dependency.version.hcann = ${hibernateCommonsAnnotationVersion}
dependency.version.tika = ${tikaVersion}
test.hibernate.orm.module.slot = ${test.hibernate.orm.module.slot}
test.hibernate.search.module.slot = ${test.hibernate.search.module.slot}
dependency.version.HibernateSearch.builtin = ${builtinVersion}
