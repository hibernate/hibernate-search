#This is to let Maven properties be injected in the file, so that we can use those in DataSourceConfigurator
connection-url ${jdbc.url}
user-name ${jdbc.user}
password ${jdbc.pass}
driver-name ${jdbc.driver.artifactId}-${jdbc.driver.version}.jar
transaction-isolation ${jdbc.isolation}