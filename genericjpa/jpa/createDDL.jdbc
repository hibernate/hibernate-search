CREATE TABLE SORCERER (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PLACE (ID INTEGER NOT NULL, COOL TINYINT(1) default 0, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE CustomUpdatedEntity (id BIGINT NOT NULL, text VARCHAR(255), PRIMARY KEY (id))
CREATE TABLE MultipleColumnsIdEntity (secondId VARCHAR(100), firstId VARCHAR(100), info VARCHAR(255), PRIMARY KEY (secondId, firstId))
CREATE TABLE Place_Sorcerer (Sorcerer_ID INTEGER NOT NULL, place_ID INTEGER NOT NULL, PRIMARY KEY (Sorcerer_ID, place_ID))
ALTER TABLE Place_Sorcerer ADD CONSTRAINT FK_Place_Sorcerer_place_ID FOREIGN KEY (place_ID) REFERENCES PLACE (ID)
ALTER TABLE Place_Sorcerer ADD CONSTRAINT FK_Place_Sorcerer_Sorcerer_ID FOREIGN KEY (Sorcerer_ID) REFERENCES SORCERER (ID)
