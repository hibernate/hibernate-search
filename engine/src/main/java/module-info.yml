name: org.hibernate.search.engine

requires:
  - module: org.jboss.logging

  - module: org.hibernate.search.util.common
    transitive: true

uses:
  - org.hibernate.search.engine.environment.bean.spi.BeanConfigurer

exports:
  - package: org\.hibernate\.search(\..+)*\.(logging|reporting)\.impl
    pattern: true
    to:
      - org.jboss.logging
      - org.hibernate.search.engine
  - package: org\.hibernate\.search(\..+)*\.impl
    pattern: true
    to:
      - org.hibernate.search.engine