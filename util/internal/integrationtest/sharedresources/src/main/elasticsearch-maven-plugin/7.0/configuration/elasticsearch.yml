network.host: _local_

# Avoid warnings related to clustering
discovery.type: single-node

# Prevent swapping
bootstrap.memory_lock: true

# Disable starting multiple nodes on a single system:
node.max_local_storage_nodes: 1
